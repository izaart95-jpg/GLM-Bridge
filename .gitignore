# Built binaries
zai-api
zai-bridge
token-collector

# Runtime artifacts (tokens.sqlite holds harvested device tokens — never commit)
tokens.sqlite
*.log

# Local environment
.env

# Local reference checkout (symlink to DeepseekFreeAPI)
reference
